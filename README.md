# Customer-Churn-Prediction
Built a customer churn prediction system using a neural network implemented entirely from scratch with NumPy. Developed the end-to-end ML pipeline including preprocessing, forward and backward propagation, gradient descent optimization, and model evaluation to identify customers likely to discontinue services.
